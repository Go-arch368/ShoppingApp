import { View } from 'react-native';
import React from 'react';
import BannerGrid from './BannerGrid';

const Section = () => {
  return (
    <View className="bg-teal ">
      <BannerGrid/>
    </View>
  );
};

export default Section;
